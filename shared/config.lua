Config = {}
Config.Locale = 'en'
Config.WaterHeight = 1.0 -- if above this height,it will activate the shark before a warning..Activated = function(sender, index)

Translation = {
    ['en'] = {

    }
}